# EDP Flow branding

Place deployment-specific brand assets here.

Planned app asset:

- `edp-logo.png` — EDP logo for the EDP Flow sign-in experience and related store/legal presentation.

Keep original/source artwork outside generated platform folders where possible. Export app-ready raster/SVG assets deliberately so white-label deployments can swap branding without changing the shared SPRK42 Flow product identity or repository structure.
